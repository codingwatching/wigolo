import type { CategoryDef } from './types.js';

export const agentsCategory: CategoryDef = {
  id: 'agents',
  label: 'Agents',
  description: 'Agent CLIs that share wigolo settings',
  fields: [],
};
