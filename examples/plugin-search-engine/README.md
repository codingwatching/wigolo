# Minimal search engine plugin example

Copy this directory as a starting point for a `searchEngine` plugin.

- `package.json` points `main` at `index.mjs`
- `index.mjs` exports a single `searchEngine` object
- The object matches the loader's required `SearchEngine` shape
